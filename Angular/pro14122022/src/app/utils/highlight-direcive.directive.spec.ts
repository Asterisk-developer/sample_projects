import { HighlightDirecive } from './highlight-direcive.directive';

describe('HighlightDirecive', () => {
  it('should create an instance', () => {
    const directive = new HighlightDirecive();
    expect(directive).toBeTruthy();
  });
});
